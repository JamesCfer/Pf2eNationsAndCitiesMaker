<div class="pf2e-leadership">

  <div class="pf2e-leadership-mayor">
    <h4><i class="fa-solid fa-crown"></i> Head of {{settlement.kind}}</h4>
    <div class="pf2e-leadership-mayor-row">
      <input type="text" data-settlement-path="government.leaderName" value="{{settlement.government.leaderName}}" />
      <input type="text" data-settlement-path="government.type"       value="{{settlement.government.type}}" />
    </div>
  </div>

  <h4><i class="fa-solid fa-user-group"></i> Council</h4>
  {{#if settlement.leadership.length}}
    <ul class="pf2e-leadership-list">
      {{#each settlement.leadership}}
        <li class="pf2e-leader-card">
          <input type="text" class="pf2e-leader-title" data-settlement-path="leadership.{{@index}}.title" value="{{title}}" />
          <input type="text" class="pf2e-leader-name"  data-settlement-path="leadership.{{@index}}.name"  value="{{name}}" />
          <input type="text" class="pf2e-leader-role"  data-settlement-path="leadership.{{@index}}.role"  value="{{role}}" placeholder="role / domain" />
          {{#if actorId}}
            <button type="button" class="pf2e-icon-btn"
                    data-action="openLinkedActor" data-actor-id="{{actorId}}" title="Open actor">
              <i class="fa-solid fa-user"></i>
            </button>
          {{/if}}
          {{#if ../canGenerateNpc}}
            <button type="button" class="pf2e-icon-btn"
                    data-action="generateLeader" data-index="{{@index}}" title="Generate NPC for this leader">
              <i class="fa-solid fa-wand-magic-sparkles"></i>
            </button>
          {{/if}}
          <button type="button" class="pf2e-icon-btn pf2e-icon-btn--danger"
                  data-action="removeLeader" data-index="{{@index}}" title="Remove leader">
            <i class="fa-solid fa-xmark"></i>
          </button>
        </li>
      {{/each}}
    </ul>
  {{else}}
    <em class="pf2e-empty">No council members yet.</em>
  {{/if}}

  <button type="button" class="pf2e-settlement-btn" data-action="addLeader">
    <i class="fa-solid fa-user-plus"></i> Add Council Member
  </button>

  <h4><i class="fa-solid fa-place-of-worship"></i> Religions</h4>
  {{#if settlement.religions.length}}
    <ul class="pf2e-religion-list">
      {{#each settlement.religions}}
        <li class="pf2e-religion-card">
          <input type="text" class="pf2e-religion-name"
                 data-settlement-path="religions.{{@index}}.name" value="{{name}}" />
          <label class="pf2e-religion-label">Followers
            <input type="number" class="pf2e-religion-followers"
                   data-settlement-path="religions.{{@index}}.followers" value="{{followers}}" min="0" />
          </label>
          <label class="pf2e-religion-label">Influence
            <input type="number" class="pf2e-religion-influence"
                   data-settlement-path="religions.{{@index}}.influence" value="{{influence}}" min="0" max="100" />
          </label>
          <label class="pf2e-religion-label">Temple
            <select data-settlement-path="religions.{{@index}}.templeStoreId" data-nullable-str>
              <option value="">— None —</option>
              {{#each ../settlement.stores}}
                {{#if (eqStr type "temple")}}
                  <option value="{{id}}" {{#if (eqStr id ../templeStoreId)}}selected{{/if}}>{{name}}</option>
                {{/if}}
              {{/each}}
            </select>
          </label>
          <button type="button" class="pf2e-icon-btn pf2e-icon-btn--danger"
                  data-action="removeReligion" data-religion-id="{{id}}" title="Remove religion">
            <i class="fa-solid fa-xmark"></i>
          </button>
        </li>
      {{/each}}
    </ul>
  {{else}}
    <em class="pf2e-empty">No religions tracked yet.</em>
  {{/if}}

  <button type="button" class="pf2e-settlement-btn" data-action="addReligion">
    <i class="fa-solid fa-plus"></i> Add Religion
  </button>

  <h4><i class="fa-solid fa-people-arrows"></i> Factions
    <span class="pf2e-faction-total {{#if (neqNum factionsTotal 100)}}pf2e-faction-total--warn{{/if}}">
      {{factionsTotal}}% of influence assigned
    </span>
  </h4>
  {{#if settlement.factions.length}}
    <ul class="pf2e-faction-list">
      {{#each settlement.factions}}
        <li class="pf2e-faction-card">
          <input type="text" class="pf2e-faction-name"
                 data-settlement-path="factions.{{@index}}.name" value="{{name}}" />
          <select data-settlement-path="factions.{{@index}}.type">
            <option value="church"   {{#if (eqStr type "church")}}selected{{/if}}>Church</option>
            <option value="guild"    {{#if (eqStr type "guild")}}selected{{/if}}>Guild</option>
            <option value="nobles"   {{#if (eqStr type "nobles")}}selected{{/if}}>Nobles</option>
            <option value="criminal" {{#if (eqStr type "criminal")}}selected{{/if}}>Criminal</option>
            <option value="crown"    {{#if (eqStr type "crown")}}selected{{/if}}>Crown</option>
          </select>
          <label class="pf2e-faction-label">Influence
            <input type="number" class="pf2e-faction-influence"
                   data-settlement-path="factions.{{@index}}.influence" value="{{influence}}" min="0" max="100" />
          </label>
          <div class="pf2e-demographic-bar-wrap">
            <div class="pf2e-demographic-bar" style="width:{{influence}}%;"></div>
          </div>
          <button type="button" class="pf2e-icon-btn pf2e-icon-btn--danger"
                  data-action="removeFaction" data-faction-id="{{id}}" title="Remove faction">
            <i class="fa-solid fa-xmark"></i>
          </button>
        </li>
      {{/each}}
    </ul>
  {{else}}
    <em class="pf2e-empty">No factions tracked yet.</em>
  {{/if}}

  <button type="button" class="pf2e-settlement-btn" data-action="addFaction">
    <i class="fa-solid fa-plus"></i> Add Faction
  </button>
</div>
